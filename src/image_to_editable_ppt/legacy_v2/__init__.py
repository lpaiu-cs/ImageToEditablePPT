"""Reserved namespace for the isolated v2 conversion core.

Phase 0~1 keeps legacy modules at their current import paths to avoid breaking
tests and tooling. This package exists so the migration target is explicit.
"""
