Semantic-first validation artifacts are written here.

Default runner:

```bash
python tools/alignment_loop.py input.png
```

The runner now writes iterations into `workbench2.0/input-alignment/iter_XX/`.
