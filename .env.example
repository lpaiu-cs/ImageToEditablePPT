# Copy to `.env` and fill in the provider details before running the semantic-first pipeline.
VLM_API_KEY=
VLM_MODEL=gpt-4o
VLM_BASE_URL=https://api.openai.com/v1/chat/completions
VLM_TIMEOUT_SECONDS=90
